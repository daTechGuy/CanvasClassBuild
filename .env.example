# ClassBuild — API Keys
# Keys are entered through the app's Setup page (not read from environment variables).
# This file documents what you'll need.

# Required — Anthropic Claude API key
ANTHROPIC_API_KEY=sk-ant-...

# Optional — Google Gemini API key for infographics and voice narration (TTS)
GEMINI_API_KEY=
